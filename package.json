{
  "name": "trainticket",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "format": "prettier --write .",
    "check": "prettier --check ."
  },
  "dependencies": {
    "@prisma/client": "^5.18.0",
    "hono": "^4.5.5",
    "prisma": "^5.18.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "3.3.3"
  }
}
